diff --git a/src/app/features/reports/reports.component.css b/src/app/features/reports/reports.component.css
index 1879747..4206256 100644
--- a/src/app/features/reports/reports.component.css
+++ b/src/app/features/reports/reports.component.css
@@ -22,6 +22,77 @@
 .page-header-actions { padding-top: 4px; }
 .preview-actions { display: flex; gap: 8px; flex-wrap: wrap; }
 
+/* ── Summary stat cards ────────────────────────────────────
+   Four-up overview row (Total / Pending / Dispatched / Critical).
+   Colors ride the same semantic tokens used everywhere else
+   (--primary-*, --lis-warning, --lis-primary, --lis-danger) so
+   the row re-themes automatically across light/dark and every
+   palette (ocean, sunset, violet) without any local overrides. */
+.reports-stats-grid {
+  display: grid;
+  grid-template-columns: repeat(4, 1fr);
+  gap: 16px;
+  margin-bottom: 20px;
+}
+@media (max-width: 1100px) {
+  .reports-stats-grid { grid-template-columns: repeat(2, 1fr); }
+}
+@media (max-width: 560px) {
+  .reports-stats-grid { grid-template-columns: 1fr; }
+}
+
+.rsc-card {
+  background: var(--card-bg);
+  border: 0.5px solid var(--border-color);
+  border-radius: var(--lis-card-radius);
+  padding: 16px 18px;
+  box-shadow: var(--shadow-card, 0 1px 3px rgba(0,0,0,0.06));
+  transition: transform 0.14s ease, box-shadow 0.14s ease;
+}
+.rsc-card:hover {
+  transform: translateY(-1px);
+  box-shadow: var(--shadow-card-hover, 0 8px 24px rgba(0,0,0,0.10));
+}
+
+.rsc-top { display: flex; align-items: flex-start; gap: 12px; }
+
+.rsc-icon {
+  width: 40px;
+  height: 40px;
+  border-radius: 10px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-size: 19px;
+  flex-shrink: 0;
+}
+.rsc-icon--primary { background: var(--primary-xlight); color: var(--primary-deeper); }
+.rsc-icon--warning  { background: rgba(239,159,39,0.14); color: var(--lis-warning); }
+.rsc-icon--success  { background: var(--lis-primary-xlight); color: var(--lis-primary); }
+.rsc-icon--danger   { background: var(--lis-danger-xlight); color: var(--lis-danger); }
+
+.rsc-body { flex: 1; min-width: 0; }
+.rsc-label {
+  font-size: 11.5px;
+  color: var(--text-secondary);
+  margin-bottom: 3px;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+.rsc-value { font-size: 22px; font-weight: 600; color: var(--text-primary); line-height: 1.15; }
+
+.rsc-spark { width: 64px; height: 26px; flex-shrink: 0; margin-top: 2px; }
+.rsc-spark--primary polyline { stroke: var(--primary-deeper); }
+.rsc-spark--warning  polyline { stroke: var(--lis-warning); }
+.rsc-spark--success  polyline { stroke: var(--lis-primary); }
+.rsc-spark--danger   polyline { stroke: var(--lis-danger); }
+
+.rsc-delta { font-size: 11px; font-weight: 500; margin-top: 10px; }
+.rsc-delta--up      { color: var(--lis-primary); }
+.rsc-delta--down     { color: var(--lis-danger); }
+.rsc-delta--neutral  { color: var(--lis-warning); }
+
 /* ── Layout ──────────────────────────────────────────────
    Grid sizing: the list column is wide enough that name +
    meta + status pill never collide. minmax(0, 1fr) on the
diff --git a/src/app/features/reports/reports.component.html b/src/app/features/reports/reports.component.html
index da50806..0310007 100644
--- a/src/app/features/reports/reports.component.html
+++ b/src/app/features/reports/reports.component.html
@@ -24,6 +24,26 @@
   }
 </div>
 
+<section class="reports-stats-grid" aria-label="Report summary">
+  @for (s of stats; track s.id) {
+    <div class="rsc-card">
+      <div class="rsc-top">
+        <div class="rsc-icon" [class]="'rsc-icon--' + s.variant" aria-hidden="true">
+          <i class="ti ti-{{ s.icon }}"></i>
+        </div>
+        <div class="rsc-body">
+          <div class="rsc-label">{{ s.label }}</div>
+          <div class="rsc-value">{{ s.value }}</div>
+        </div>
+        <svg class="rsc-spark" [class]="'rsc-spark--' + s.variant" viewBox="0 0 100 32" preserveAspectRatio="none" aria-hidden="true">
+          <polyline [attr.points]="s.spark" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
+        </svg>
+      </div>
+      <div class="rsc-delta" [class]="'rsc-delta--' + s.deltaType">{{ s.delta }}</div>
+    </div>
+  }
+</section>
+
 <div class="reports-layout">
   <!-- Report list -->
   <div class="lis-card report-list">
diff --git a/src/app/features/reports/reports.component.ts b/src/app/features/reports/reports.component.ts
index 3558bfd..4d5a9a4 100644
--- a/src/app/features/reports/reports.component.ts
+++ b/src/app/features/reports/reports.component.ts
@@ -7,6 +7,24 @@ interface ReportRow {
   test: string; date: string; status: 'pending' | 'dispatched';
 }
 
+interface ReportStat {
+  id: string;
+  label: string;
+  value: string;
+  icon: string;
+  variant: 'primary' | 'warning' | 'success' | 'danger';
+  delta: string;
+  deltaType: 'up' | 'down' | 'neutral';
+  spark: string; // normalized 0-100 points, space separated "x,y x,y ..."
+}
+
+const REPORT_STATS: ReportStat[] = [
+  { id: 'total',      label: 'Total Reports',   value: '1,248', icon: 'file-text',      variant: 'primary', delta: '+12.5% vs last month', deltaType: 'up',      spark: '0,28 16,24 32,26 48,18 64,20 80,10 100,6' },
+  { id: 'pending',    label: 'Pending Reports', value: '5',     icon: 'hourglass',      variant: 'warning', delta: 'Needs attention',       deltaType: 'neutral', spark: '0,20 16,10 32,16 48,8 64,18 80,12 100,4' },
+  { id: 'dispatched', label: 'Dispatched',      value: '320',   icon: 'circle-check',   variant: 'success',  delta: '+8.2% vs last month',   deltaType: 'up',      spark: '0,26 16,22 32,24 48,14 64,18 80,8 100,10' },
+  { id: 'critical',   label: 'Critical Alerts', value: '12',    icon: 'alert-triangle', variant: 'danger',  delta: 'Requires follow-up',    deltaType: 'down',    spark: '0,10 16,18 32,12 48,22 64,14 80,24 100,18' },
+];
+
 const REPORTS: ReportRow[] = [
   { id:'RPT-2406-041', patientName:'Arun Pillai',   patientId:'PAT-0316', test:'KFT',          date:'23 Jun 2026', status:'pending' },
   { id:'RPT-2406-040', patientName:'Priya Sharma',  patientId:'PAT-0315', test:'Thyroid panel', date:'23 Jun 2026', status:'pending' },
@@ -37,6 +55,7 @@ const KFT_ROWS = [
 export class ReportsComponent {
   protected readonly reports  = REPORTS;
   protected readonly kftRows  = KFT_ROWS;
+  protected readonly stats    = REPORT_STATS;
   selected = signal<ReportRow | null>(REPORTS[0]);
   query = signal('');
 
